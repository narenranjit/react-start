/**
 * Test store addons
 */

// import expect from 'expect';

describe('configureStore', ()=> {
    it('should contain an object for async reducers', ()=> {
        expect(1).to.equal(1);
    });
});
